# 採用具權威性的確定性規則引擎

規則引擎是唯一能改變單局狀態的元件：客戶端與 bot 查詢合法玩家行動、提交選定的行動，再由引擎回傳新狀態與遊戲事件。所有隨機性皆由可重現且可指定 seed 的來源提供；CLI、bot 與持久化職責均位於引擎之外。如此能讓所有操控端共用同一條規則路徑，並使單局可測試、可重播，同時不強迫持久化模型採用 event sourcing。

## 曾考慮的方案

允許 CLI、bot 或個別卡牌整合直接修改共用狀態，初期所需結構較少，但會造成規則驗證重複，並大幅提高確定性重播的難度。
