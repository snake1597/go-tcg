# 將儲存基礎設施置於規則引擎之外

規則引擎保持純記憶體運作，不依賴 PostgreSQL、GORM 或 Redis；首個可玩 CLI 版本不使用資料庫。後續需要保存牌組、對局摘要與 replay 時，先在外部 adapter 使用 PostgreSQL 與 GORM；Redis 只有在伺服器、跨程序進行中對局、配對或分散式協調產生具體需求後才導入，不預先建立快取抽象。

## 曾考慮的方案

從首版起強制同時使用 PostgreSQL 與 Redis 可以提早固定技術組合，但單機 CLI 對局沒有相應需求，反而會增加部署、故障模式與測試成本，並使核心規則受到外部系統影響。
